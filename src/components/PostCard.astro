---
import type { CollectionEntry } from "astro:content";
import { getRelativeLocaleUrl } from "astro:i18n";
import { getPostUrl } from "@/utils/getPostPaths";
import { slugifyStr } from "@/utils/slugify";
import { toTransitionName } from "@/utils/toTransitionName";
import { useTranslations, tplStr } from "@/i18n";
import { getReadingTime } from "@/utils/readingTime";
import config from "@/config";
import Datetime from "./Datetime.astro";
import Tag from "./Tag.astro";
import Clock from "@/assets/icons/Clock.astro";
import Card from "@/components/ui/Card.astro";
import CardTitle from "@/components/ui/CardTitle.astro";
import CardDescription from "@/components/ui/CardDescription.astro";
import { cn } from "@/lib/utils";

interface Props {
  post: CollectionEntry<"posts">;
  /** 是否显示标签 */
  showTags?: boolean;
  class?: string;
}

const { post, showTags = true, class: className } = Astro.props;
const { data, id, filePath } = post;
const locale = Astro.currentLocale ?? config.site.lang;
const tr = useTranslations(locale);
const href = getPostUrl(id, filePath, locale);
const tags = data.tags.map((tag) => ({
  tag: slugifyStr(tag),
  tagName: tag,
}));
const transitionName = toTransitionName(data.title);
/** 阅读时长（分钟）：按当前语言的字符/单词速率估算 */
const readingMinutes = getReadingTime(post.body ?? "", locale);
/** 分类链接：设置 category 时生成对应分类页地址 */
const categorySlug = data.category ? slugifyStr(data.category) : undefined;
const categoryHref = categorySlug
  ? getRelativeLocaleUrl(locale, `categories/${categorySlug}/`)
  : undefined;
---

<Card
  class={cn(
    "group relative flex h-80 flex-col justify-evenly gap-0 overflow-hidden transition-colors hover:bg-accent/30",
    className
  )}
>
  {/* 标题行：最多两行截断，保证高度稳定 */}
  <CardTitle
    transition:name={transitionName}
    class="mx-4 line-clamp-2 leading-snug transition-colors group-hover:text-primary"
  >
    <a href={href} class="after:absolute after:inset-0">{data.title}</a>
  </CardTitle>

  {/* 文章信息行：发布时间与阅读时长 */}
  <div class="mx-4 flex flex-wrap items-center gap-x-3 gap-y-1">
    <Datetime
      pubDatetime={data.pubDatetime}
      modDatetime={data.modDatetime}
      timezone={data.timezone}
      size="sm"
    />
    <span class="flex items-center gap-x-1 text-muted-foreground">
      <Clock class="size-3.5 shrink-0" />
      <span class="text-sm">
        {tplStr(tr.post.readingTime, { minutes: readingMinutes })}
      </span>
    </span>
  </div>

  {/* 描述行：默认三行空间，超出自动截断 */}
  {data.description && (
    <CardDescription class="mx-4 line-clamp-3">{data.description}</CardDescription>
  )}

  {/* 分类与标签行：相对定位提升层级，保持独立可点击 */}
  {
    showTags && (tags.length > 0 || categoryHref) && (
      <div class="relative z-10 mx-4 flex flex-wrap items-center gap-2">
        {categoryHref && (
          <a
            href={categoryHref}
            class="inline-flex items-center rounded-full bg-primary/10 px-2.5 py-0.5 text-xs font-medium text-primary transition-colors hover:bg-primary/20"
          >
            {data.category}
          </a>
        )}
        {tags.map((t) => (
          <Tag tag={t.tag} tagName={t.tagName} size="sm" />
        ))}
      </div>
    )
  }
</Card>
